name: 🚀 Simple Proxy Updater

on:
  workflow_dispatch:
  schedule:
    - cron: '0 */2 * * *'

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
      
    - name: 📝 Create proxy files
      run: |
        # Create basic proxies.txt
        cat > proxies.txt << EOF
# Singapore & Malaysia Proxies
# Updated: $(date)

# 🇸🇬 Singapore Proxies
13.250.131.37:443 | Singapore
54.255.185.94:443 | Singapore

# 🇲🇾 Malaysia Proxies  
52.77.225.242:443 | Malaysia
13.76.157.24:443 | Malaysia

# Total: 4 proxies
EOF

        # Create simple list
        cat > proxies_simple.txt << EOF
# Simple Proxy List
13.250.131.37:443
54.255.185.94:443
52.77.225.242:443
13.76.157.24:443
EOF

        echo "✅ Files created successfully"
        
    - name: 💾 Commit changes
      run: |
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        git add proxies.txt proxies_simple.txt
        git commit -m "🚀 Update proxies [$(date +%H:%M)]" || echo "No changes to commit"
        git push
